# Puerto donde escucha el servidor
PORT=8080

# Entorno de ejecucion: development | production
NODE_ENV=development

# Cadena de conexion a MongoDB
MONGO_URL=mongodb://localhost:27017/eventos

# Secreto para firmar los JWT (proximas entregas)
JWT_SECRET=cambiar_este_secreto
